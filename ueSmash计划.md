# 项目计划书：基于UE的多人乱斗游戏开发

## 1. 项目概述

- **项目名称**：[暂称《UE大乱斗》]

- **游戏类型**：3D横版、多人乱斗格斗游戏

- **核心玩法**：受《任天堂明星大乱斗》启发，采用“击飞值”而非“血条”制胜机制。玩家目标是通过攻击增加对手的击飞值，最终将其打飞出战场平台

  。

- **技术基础**：基于Unreal Engine 5（版本5.6），利用其强大的网络同步、动画蓝图和物理系统。

- **开发团队**：2名程序开发者（兼顾逻辑、网络、AI、部分策划与美术资源整合）。

- **项目愿景**：打造一款核心玩法扎实、网络同步流畅、支持最多4人同屏对战、具备扩展性的独立格斗游戏。

## 2. 核心游戏设计（程序视角）

### 2.1 战斗系统

这是项目的绝对核心，所有设计都围绕其展开。

- **击飞值**：

  - **机制**：替代传统血条。角色受攻击后，击飞值百分比提升，数值越高，受击时被击飞的距离越远

    。

  - **实现**：一个可复制的浮点变量，作为角色状态的核心参数。击飞效果通过物理计算实现（击飞值 × 攻击力度 × 攻击方向 - 角色重量）。

- **攻击系统**：

  - **普通攻击**：轻攻击、重攻击（可蓄力，蓄力时角色发光提示

    ）、空中攻击（前、后、上、下）。

  - **特殊攻击**：每个角色独有的招牌技能，对应方向+特殊键。

  - **实现**：通过动画蒙太奇管理不同攻击动作，并在动画关键帧触发碰撞体检测。

- **移动与防御**：

  - **移动**：左右移动、奔跑、跳跃（最多二段跳，部分角色可多段

    ）。

  - **防御**：护盾防御（消耗能量条，被攻击会缩小，破裂会眩晕

    ）、闪避（地面闪身、空中闪避，带短暂无敌帧）。

- **胜负判定**：玩家被击飞出屏幕边界且无法返回则判负。最后存活的玩家获胜。

### 2.2 角色系统

- **角色属性**：定义基础属性，如**重量**（影响击飞难度和受击后移动

  ）、**移动速度**、**跳跃力**等。这些将是角色平衡的关键数值。

- **角色数据资产**：为每个角色创建专属的数据资产，存储其技能参数、招式判定框、移动速度等，方便调试和平衡。

### 2.3 舞台设计

- **基础结构**：包含主要平台和可能存在的移动平台、危险区域。
- **边界**：明确的可击飞边界，角色超出边界后在一定时间内未返回则判定为出局。

## 3. 技术架构与网络同步方案

本项目为多人对战游戏，**网络同步是技术核心的重中之重**

。

### 3.1 网络模型

- **架构**：采用UE原生的**客户端-服务器模型**。指定一个客户端作为**监听服务器**，其他客户端连接至该服务器

  。这是保证公平性和逻辑一致性的最佳选择。

- **权威性**：**服务器是游戏状态的唯一权威**。所有关键逻辑（伤害计算、胜负判定、道具生成）必须在服务器上执行

  。客户端只负责输入采集和表现。

### 3.2 同步策略

- **角色移动**：使用UE内置的`Character Movement Component`的复制功能，但需进行优化以减少延迟带来的粘滞感。

- **攻击同步**：

  - **客户端预测**：玩家输入攻击指令后，本地立即播放动画表现，提升响应速度。

  - **服务器校验**：攻击指令和输入时间戳发送至服务器，服务器在权威时间轴上重演判定。若有效，则将结果（命中目标、伤害、击飞方向）同步给所有客户端

    。

- **关键RPC调用**：使用**可靠RPC**传输关键事件，如角色死亡、游戏开始/结束。使用**不可靠RPC**传输频繁更新但不致命的数据，如角色动画状态。

### 3.3 延迟处理

- **插值与外推**：对非本地角色进行运动插值，使其移动平滑。
- **一致性**：确保所有客户端的随机数种子同步，保证道具生成、击退方向等随机事件在所有机器上一致。

# 开发计划

### 第一阶段：网络化的核心战斗原型

此阶段的目标是创建一个**最基本的、但完全具备网络同步功能的对战原型**。所有功能从一开始就在网络环境下开发。

- **并行启动：基础框架与网络设置**

  - **项目初始化与网络配置**：创建项目后，立即在项目设置中配置网络模式为“Client-Server”。创建基本的`GameMode`、`PlayerState`、`GameState`等类，并为它们配置基础的复制属性

    。

  - **核心角色类**：创建角色类，在其构造函数中立即设置 `bReplicates = true`，这是Actor能够进行网络同步的总开关

    。配置`CharacterMovementComponent`并设置`bReplicateMovement = true`以实现基础移动同步。

- **网络化的移动与输入**

  - **移动同步**：实现基础的左右移动和跳跃。由于设置了`bReplicateMovement`，角色的移动会通过UE的属性同步机制自动由服务器同步给所有客户端

    。

  - **输入处理**：确保玩家的输入在`PlayerController`或`Pawn`中处理，为后续的RPC调用做好准备。

- **网络化的攻击与击飞系统**

  - **攻击指令（RPC）**：当玩家发动攻击时，使用**Server RPC**将输入指令从客户端发送到服务器。这是确保攻击逻辑权威性的关键一步

    。函数应包含`WithValidation`以进行安全检查

    。

  - **攻击效果（属性同步与Multicast RPC）**：

    - **方案1（属性同步）**：在服务器上计算击飞值，将其设置为一个使用`Replicated`或`RepNotify`标记的属性。当该属性同步到客户端时，在`OnRep`函数中执行击飞逻辑

      。

    - **方案2（Multicast RPC）**：服务器在计算完击飞值后，调用一个**Multicast RPC**，让所有客户端同时执行击飞表现。这对于需要同时触发的效果（如爆炸）更合适

      。

  - **胜负判定**：胜负判定逻辑必须放在服务器端的权威`GameMode`中。当服务器判定一名玩家出局后，通过RPC或复制属性通知所有客户端更新UI和状态

    。

### 🔄 贯穿始终的持续验证循环

从第一行代码开始，就必须建立严格的验证流程，这正是你强调的核心。

- **日常开发流程**：任何新功能的开发，都应遵循 **“在监听服务器模式下启动，并连接一个客户端进行测试”** 的流程。立即验证新功能的同步是否正确。

- **网络调试工具**：熟练使用UE内置的**Network Profiler**和`LogNet`输出，实时分析网络流量、定位同步延迟或数据包丢失问题

  。

- **模拟恶劣网络环境**：在开发过程中，定期使用控制台命令（如 `Net PktLag=300`模拟延迟，`Net PktLoss=20`模拟丢包）来测试游戏的健壮性，确保在不良网络下仍能保持逻辑一致，不发生致命错误

  。

### 🧩 第二阶段：系统深化与内容扩展

在核心玩法网络同步稳定后，开始构建更复杂的系统，这些系统同样需要从一开始就设计为网络化的。

- **角色与技能系统****角色数据资产**：创建数据资产来管理角色属性。这些资产本身不需要复制，但角色应用这些数据后产生的状态变化（如当前生命值）需要同步。**特殊技能**：每个特殊技能的实现，都必须复用第一阶段的“Server RPC处理指令 -> 服务器权威计算 -> 属性同步/Multicast RPC广播结果”模式。

- **UI系统的网络化**

  - **游戏内HUD**：在`PlayerState`或`GameState`中创建复制属性来存储比分、击飞值等信息。在UI控件中绑定这些属性，或监听其`OnRep`事件来自动更新界面

    。

- **高级网络优化**

  - **同步频率与优先级**：根据Actor的重要性，调整`NetUpdateFrequency`和`NetPriority`，优化网络带宽占用

    。

  - **网络相关性**：正确设置Actor的`NetCullDistance`和相关性规则，确保远端不可见的物体不同步，进一步提升效率

    。

### ⚙️ 第三阶段：高级功能与优化

此阶段着重于提升游戏的完整性和体验流畅度。

- **AI机器人（网络化）**：使用行为树编写AI逻辑。AI控制的角色本质上是一个“模拟玩家”，其输入和决策应由服务器权威控制，然后将行动结果同步给各客户端。

- **输入预测与回滚**：为了在高延迟下提供更流畅的体验，可以实现客户端预测。对于移动等非关键操作，允许客户端预测执行，服务器随后进行校验和纠正

  。

- **断线重连**：利用UE的机制，将关键持久化数据（如玩家分数）存储在`PlayerState`中。当玩家重连时，服务器会自动将这些复制属性同步给新客户端，使其恢复状态